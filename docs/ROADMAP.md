# ROADMAP.md

フェーズは順に完成させる。前フェーズの完了条件を満たすまで次の大規模機能へ進まない。

> **現在地（2026-07 更新）**: Phase 0 ✅ / Phase 1 ✅ / **Phase 2 進行中（核は実装済み）** / エンドゲーム 未着手。
> Phase 2 の核「素材を集めて作る（超絶レア泥素材→特別装備クラフト）」は、
> **MH式ボス狩猟 → ボス固有素材 → 専用装備クラフト**として実装済み。

## 設計変更ログ（当初仕様からの意図的な変更）
- **見た目は「職業固定」に変更**（当初の“装備で見た目が変わる”ペーパードール装備レイヤーは廃止）。
  職業ごとに固定の本体スプライト、装備は**性能のみ**。1次職5職の絵を投入済み。理由: 装備アート量産の負荷回避＋職業アイデンティティ。
- **エンドコンテンツ方針をMHに寄せた**: ボスは全て「大型狩猟クエスト」化（専用アリーナに出現・反復で素材周回）。
  掲示板は「通常/大型狩猟」の2枠＋★ランク（1〜7）。
- UI: ドットフォント（DotGothic16）導入、共通テーマ集約、起動時「ご注意」画面。

## Phase 0 — 実機検証用の縦切り（✅ 完了）

目的: 大量コンテンツ前に、モバイル PWA の基盤が実機で動くことを確認する。

含むもの（実装済み）:
- 縦画面の町テストマップ / プレイヤー移動 / 仮想スティック
- 通常攻撃 / スキルボタン1 / 敵1種 / 素材ドロップ1種
- 頭・胴・武器の装備レイヤー（PaperDollAnimator）/ 装備変更画面
- IndexedDB セーブ・ロード / 自動保存（可視性・装備変更・定期）
- PWA（standalone / portrait / オフライン / 更新通知）
- iPhone/Android の画面比率対応 / 横画面警告 / safe-area

**完了条件（実機）**: 移動→攻撃→スキル→敵撃破→素材ドロップ→装備変更で見た目変化→保存→再起動でロード→オフライン起動→縦横対応が、iPhone Safari/PWA と Android Chrome/PWA でエラーなく動く。

検証コマンド（CI/開発）: `npm run typecheck && npm run validate-data && npm run test && npm run build`。

> Phase 0 が実機で安定するまで、職業・ペット・大量の装備・複数マップを追加しない。

## Phase 1 — 縦切り試作（✅ ほぼ完了・超過）

町1・フィールド1・ダンジョン1・ボス部屋1、通常敵3+ボス1、初期職+1次職1、アクティブ2+パッシブ1、素材8/装備10〜12/レシピ6/消耗品2、ペット1、簡単なNPC会話、ローカルセーブ、装備による見た目変更、ステータス割り振り、スキル習得、1次職転職。UI 一式（仮UIで可、マウス/パッド対応構造）。デバッグメニュー（リリースで無効化）。

**完了条件**: 指示の18ステップ通し（新規開始→フィールド→撃破→素材→Lvup→STR割振→スキル習得→帰town→製作→装備→能力値&見た目変化→ペット同行→ダンジョン→ボス撃破→転職→セーブ→再起動ロード→全状態維持）がエラーなく実行できる。

## Phase 2 以降（データ構造のみ先行）

tier 2〜4 職業の内容、複数ペット/進化/性格/スキル継承/**合成（最大10体）**、**レア素材クラフト（素材レアリティ＋超絶レア泥素材＋レア素材レシピ）**、追加マップ/敵/装備、状態異常の拡充、音楽/効果音、Steam 配布検討。データ構造とデバッグ表示は先に用意し、内容実装は段階的に。

> **方針決定（2026-06）**: 本作の核は「素材を集めて作る」。ハクスラの“当たり”は **超絶レア泥素材→特別装備クラフト** で表現し、完成装備のランダム直ドロップは増やさない。真のランダム泥ハクスラ（ItemInstance＋アフィックス＋マジックファインド／分解）は **クリア後（エンドゲーム）コンテンツ**として後続フェーズで投入する。詳細は `DATA_SCHEMA.md`「レア素材クラフト（Phase 2）」「ハクスラ … クリア後（エンドゲーム）」を参照。

### Phase 2 チェックリスト（2026-07 現在）
- [x] 核ループ: **MH式ボス狩猟12 → 固有素材12 → 専用装備12**（forge レシピ）
- [x] スキル系統ツリー（共通3＋系統別35＝38。前提技・必要Lv・系統ロック・職位ロック）
- [x] 職業固定の見た目（1次職5職）
- [x] ゾーン/敵/装備/レシピ増量・ボスリバランス（Lv6〜80ラダー）
- [x] クエストのカテゴリ2枠＋★ランク、UI統一（テーマ/ドットフォント/起動注意書き）
- [ ] **2次〜4次職の実コンテンツ**（スキル中身・専用要素）
- [ ] **状態異常・属性**（`element` 未使用 → 火/氷/毒 等＋耐性）← 次はここ推奨
- [ ] **ペット拡充**（複数体/進化/性格/スキル継承/合成 最大10体）
- [ ] レア素材クラフトのさらなる拡充（超絶レア泥素材の階層）
- [ ] 音楽/効果音
- [ ] 2次職以降の見た目・敵の固有絵（PixelLab で後差し）

### エンドゲーム（Phase 2 の後）
- [ ] ItemInstance のセーブ対応（ランダム個体保存）＝最大の改修点
- [ ] 調査クエスト／調査素材／R9・R10 固定装備／調査ランダム装備（テンプレ・生成・ドロップ・強化・分解）
